import { CanvasPage } from "@/components/canvas/CanvasPage";

/**
 * Canvas route - Server component that renders the canvas editor page
 */
export default function Page() {
  return <CanvasPage />;
}
